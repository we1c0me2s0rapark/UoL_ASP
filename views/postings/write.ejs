<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/public/css/common.css" rel="stylesheet" type="text/css">
    <title>Write</title>
</head>
<style type="text/css">
    form {
        /* Use column and row alignment for layout */
        display: grid;
        grid-template-columns: 1fr;
        grid-template-rows: repeat(3, auto);
    }
    button {
        width: 100px;
        height: 20px;
        margin-top: 10px;
    }
    label {
        font-weight: bold;
    }
    #divTitle {
        /* Use column and row alignment for layout */
        display: grid;
        grid-template-columns: 50px auto;
        margin-bottom: 5px;
    }
</style>
<body>
    <h1>Write Page</h1>
    <div>
        <form action="/postings/upload" method="POST">
            <!-- Content title -->
            <div id="divTitle">
                <label>Title</label>
                <input name="title" onkeypress="return event.keyCode != 13;"></input>
            </div>
            <!-- Editable text -->
            <textarea name="content" rows="35" cols="1" ></textarea>
            <!-- Button to Save the content to the database -->
            <button type="submit">Create</button>
        </form>
    </div>
</body>
</html>
